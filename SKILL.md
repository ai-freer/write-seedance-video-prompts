---
name: write-seedance-video-prompts
description: Write Seedance 2.5 video prompts and manage their project-scoped character and reference assets. Use for quick demos, long or precision videos, extensions, edits, and transitions; adapt upstream image prompts to GPT, Gemini, Seedream, or Grok Image.
---

# Seedance 2.5 Video Prompt

version: 0.3.0

把创作意图、项目资产和 Seedance 2.5 控制能力编译成可直接执行的视频生成包。只针对 Seedance 2.5 优化视频 Prompt；生成角色设定图等上游图片资产时，可适配 GPT、Gemini、Seedream 或 Grok Image。

## 必须先路由

新创作任务先确定用户选择的流程；用户或项目档案已明确时直接沿用，不重复询问：

1. **快速短片**：制作一个短而快的 demo，减少讨论和前置资产，只保留影响结果的必要确认。
2. **长视频 / 精细制作**：制作超过 30 秒的长视频，或虽短但人物一致性、产品展示、动作、镜头和成片成功率要求较高的内容。进入分镜、资产和最终生成包的分阶段确认。

在路径确定之后，询问是否已有该项目的目录和资产档案；已提供目录则直接读取。若没有，建议用 `assets/project-template/` 创建默认项目档案。不要在流程选择之前展开其他问卷。单纯解释、评审或修改已有 Prompt 时，只补缺失决策，不重启整个项目流程。

一次只追问当前最影响下一步的一个问题。可逆且低风险的细节可以推断并标注假设；主体身份、产品外观、关键动作、片长、画幅、语言等高影响项在定稿前确认。已有明确答案、批准记录和执行授权继续有效，不反复索取。

## 按需读取参考文档

- 所有任务先读 `references/workflow-router.md`，确定路径、操作类型、生成策略、规划粒度和控制锚点。
- 涉及项目目录、人物一致性、服装、场景、道具、音色或素材登记时，读 `references/project-assets.md`。
- 涉及角色设定图或其他图片资产的 Prompt、生成和验收时，读 `references/image-asset-generation.md`。
- 编译最终视频 Prompt 时，同时读 `references/prompt-compiler.md` 和 `references/seedance-2.5-reference.md`。
- 需要选择或借鉴官方案例、诊断控制失效时，读 `references/case-patterns.md` 中的匹配模式；不要搬入整套案例。
- 需要把创意变成有效镜头、设计空间/表演、安排试拍或根据成片返工时，读 [创作与迭代](references/creative-direction.md)。其中第三方案例经验不是模型能力保证。

只读取当前任务需要的章节和文件，不要把所有模板机械填满。

## 执行流程

1. **选择路径**：快速短片或长视频 / 精细制作。
2. **加载项目**：复用现有项目档案；没有时，经用户同意后从模板创建。
3. **收敛 Brief**：确认目标、片长、画幅、主要表达、主体、声音和不可改变项；用一句话说明希望观众看到后理解或感受到什么，不把风格标签当作创意本身。
4. **选择控制方案**：区分生成、续写、编辑、拼接；再选择单次生成、超长直出、基础片续写或多段生成后拼接。
5. **检查资产**：先用粗略故事/镜头需求识别主角色、Look、场景、道具和参考素材缺口，只补当前方案真正需要的资产，不为未定镜头批量制图。
6. **规划内容**：在意图驱动、节拍驱动、镜头驱动和精确时间轴之间选择最低但足够的控制粒度。需要精细控制时，为关键镜头确定叙事任务、进出状态与剪辑用途；不强迫 demo 填镜头表。
7. **编译 Prompt**：建立上传素材映射，按条件组装故事、镜头、声音、连续性和负向约束。
8. **预检并交付**：检查素材数量、时长、引用、人物一致性、动作复杂度、字幕/BGM 和编辑副作用风险。

## 两条路径的默认门槛

### 快速短片

目标是用最少输入得到可判断方向的 demo。默认只确认：

- 一句话意图、时长、画幅和用途；
- 主要主体及必须保留的外观；
- 本次最关键的动作、转折或结尾；
- 是否需要对白、环境声、音效、音乐或无声；
- 可用参考素材以及一个主要风险。

需新建主要人物时，最低资产包为：身份清晰的近景或四分之三视角图，以及当前 Look 的全身图，不强制多视图。已有足够清晰的批准资产则复用。用户知情批准本次 demo 使用候选图时，可交付标明候选风险的 demo 包，但不得把候选改记为标准资产。

### 长视频 / 精细制作

默认经过四个确认门：

1. Brief 确认；
2. 资产确认；
3. 分镜 / 镜头计划确认；
4. 最终生成包确认。

用户可以明确跳过某一门，记录风险与范围，但不能跳过实际输入上限或伪造资产批准。长视频优先拆成可独立验收的段落是本 Skill 的成本偏好，不是模型限制；用户选择超长直出时，完成相应确认后支持该策略。

## 人物一致性原则

- **主要人物**：新建时先以文字视觉规格生产并确认参考图，视频复用已批准视觉资产，不把文字“身份核”当作一致性手段。只编辑已有视频且不改变人物时，源视频本身可以承担身份锚点，不强制重做人物图包。
- **场景 Look**：从同一标准角色资产派生不同服装、妆发和场景状态。身份资产保持稳定，Look 可以按场景变化。
- **表演连续性**：稳定的行为习惯、说话方式与单镜头情绪分开记录；文字不能保证人脸或音色一致。湿衣、损伤等仅在跨镜头可见且影响叙事时补状态资产，不为每个情绪制作新 Look。
- **次要人物**：默认用 Prompt 描述年龄/族裔、肤质、面部标志、眼神、发型、服装材质、体态和情绪气质。若重复出现、承担近景/对白、参与复杂交互或一致性变重要，将其升级为受管资产。
- 不允许两个不同角色共享含混的素材引用；每次生成都要形成明确的“本次生成引用包”。

## 项目写入规则

- Skill 安装目录只存规则和空白模板；实际项目写入用户指定的独立目录，不把资产、账号信息或个人配置写回 Skill。新档案优先使用相对项目根目录的路径；沿用现有绝对路径时，不擅自移动素材。
- 独立项目可用 YAML 登记表与 Markdown 创作记录；已有导演台等档案系统时沿用其唯一主档，通过现有接口保存，不另建 YAML/TXT 平行主档。导出文件只是交换快照。本 Skill 可独立使用，不要求安装工作台。
- 保留资产来源、版本、批准状态、角色/Look 归属和使用限制。
- 新资产先登记为候选，只有用户确认或已有明确批准记录后才标记为 `approved`。
- 编写 Prompt 不等于执行生成。调用外部生成前确认授权覆盖本轮模型、资产和成本；已有明确授权不重复询问，超出范围再确认。本 Skill 不自行假定工具可用或自动执行重抽。
- 不覆盖已批准资产。创建新版本并保留关联关系。
- 关键资产、Look 或片段计划变化时，只把受影响的引用包及确认门标为待复核，不清空整个项目状态。

## 最终交付格式

每个可执行视频生成包至少包含：

1. **生成设置**：工作流路径、操作类型、策略、模式、时长与计时基准、画幅、清晰度、规划粒度，以及 demo/正式/待补素材草稿状态；
2. **上传与引用映射**：素材与 `@图片N`、`@视频N`、`@音频N` 的对应、用途与排除项；局部参考再标源片选段及目标片段范围，无素材则省略；
3. **可复制 Prompt**：只包含当前任务需要的控制模块；
4. **生成前检查**：模型边界、已知风险、待确认项和可接受假设。

若关键素材缺失、引用冲突或输入超限，先给最小补救方案，可继续交付明确标注的草稿，不能冒充可执行终包。区分真正不自洽的动作和用户有意的超现实表达；高风险不是一律禁止。能力事实、稳定建议与本 Skill 的制作偏好分开表达，不把官方展示效果当作生成保证。

复杂项目另附必要的交接说明：镜头任务、前后文与进出状态、预计剪辑用途、验收重点；这些不是模型 API 字段。工作台接入由其工作流 Skill 映射到现有对象；传回 Prompt、设置、实际引用与风险，不自动执行生成或改写批准状态。收到真实成片反馈后按创作与迭代指南决定改 Prompt、补资产、改镜头还是交给剪辑，不默认重抽。
